# Generated from the Vidchecker 8.2.2 template '- MXF AVC-I 100' (template id 30) by scripts/import_vidchecker_templates.py.
# Source export: presets/_sources/vidchecker/ (captured 2026-07-27).
#
# ALL THRESHOLDS ARE PLACEHOLDERS PENDING HUMAN APPROVAL (handoff section 30).
#
# Vidchecker checks NOT covered by this preset (no implemented
# parameter yet - see docs/vidchecker-import.md):
#   - MXF structural checks (0 sub-tests): MXF structural checks are not implemented
#   - MxfOp: MXF operational pattern is not inspected
#   - PixelAspectRatio: video.sample_aspect_ratio is planned, no detector yet
#   - FrameAspectRatio: video.display_aspect_ratio is planned, no detector yet
#   - VideoBitrate: video.bitrate is planned, no detector yet
#   - GopLength: GOP length is not inspected (no bitstream detector)
#   - FileBitrate: container.overall_bitrate is planned, no detector yet
schema_version: 1.0.0

preset:
  id: vc030_mxf_avc_i_100
  version: 1.0.0
  client: vidchecker-library
  content_type: av_delivery
  title: "MXF AVC-I 100"
  description: "Imported from the Vidchecker template '- MXF AVC-I 100'. 7 Vidchecker check(s) are not yet covered; see the preset file header."
  owner: media-operations
  status: draft
  effective_date: 2026-07-27

defaults:
  blocking: true
  severity: error

rules:
  - rule_id: container-format
    parameter_id: container.format
    operator: equals
    expected:
      value: mxf
    display_name: "Container Format"
    severity: warning
    blocking: false

  - rule_id: video-codec
    parameter_id: video.codec
    operator: equals
    expected:
      value: h264
    applies_to:
      stream_type: video
      quantifier: all
    display_name: "Video Codec (H264)"
    severity: warning
    blocking: false

  - rule_id: video-width
    parameter_id: video.width
    operator: equals
    expected:
      value: 1920
      unit: px
    applies_to:
      stream_type: video
      quantifier: all
    display_name: "Frame Width"
    severity: warning
    blocking: false

  - rule_id: video-height
    parameter_id: video.height
    operator: equals
    expected:
      value: 1080
      unit: px
    applies_to:
      stream_type: video
      quantifier: all
    display_name: "Frame Height"
    severity: warning
    blocking: false

  - rule_id: chroma-subsampling
    parameter_id: video.pixel_format
    operator: contains
    expected:
      value: "422"
    applies_to:
      stream_type: video
      quantifier: all
    display_name: "Chroma Subsampling (422)"
    description: "Checked via the pixel-format token (e.g. yuv422p10le)."
    severity: warning
    blocking: false
